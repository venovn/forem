<div class="crayons-card crayons-card--secondary p-4">
 
  <p class="color-base-70 mb-4">
    <% if Settings::Community.tagline.present? %>
      <%= Settings::Community.tagline %>
    <% else %>
      Đăng nhập và bình luận các vấn đề về sôi nổi Crypto!
    <% end %>
  </p>
  <div>
    <a href="<%= sign_up_path(state: "new-user") %>" class="crayons-btn w-100 mb-2" aria-label="Create new account">Tạo Tài Khoản</a>
    <a href="<%= sign_up_path %>" class="crayons-btn crayons-btn--ghost-brand w-100" aria-label="Log in">Đăng Nhập</a>
  </div>
</div>
